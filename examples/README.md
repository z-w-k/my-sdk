# ZHJ SDK Examples

这个目录包含了 ZHJ SDK 的示例应用，用于演示和测试 SDK 的各种功能。

## 📁 目录结构

```
examples/
├── frontend-react/     # React 前端示例
├── backend-express/    # Express 后端示例
└── README.md          # 本文件
```

## 🚀 快速开始

### 1. 安装依赖

```bash
# 在根目录安装所有依赖
pnpm install
```

### 2. 启动后端服务

```bash
# 启动 Express 后端
pnpm --filter @zhj-sdk/example-backend-express dev
```

后端服务将在 http://localhost:3001 启动

### 3. 启动前端应用

```bash
# 启动 React 前端
pnpm --filter @zhj-sdk/example-frontend-react dev
```

前端应用将在 http://localhost:3000 启动

## 📋 功能演示

### 前端 React 示例

- ✅ **HTTP 请求**: GET、POST、PUT、DELETE 等方法
- ✅ **泛型类型**: 完整的 TypeScript 类型安全
- ✅ **请求缓存**: 智能缓存机制，减少重复请求
- ✅ **请求去重**: 防止重复的并发请求
- ✅ **重试机制**: 自动重试失败的请求
- ✅ **指标收集**: 实时监控请求性能
- ✅ **响应验证**: 自动验证响应数据格式
- ✅ **请求取消**: 支持取消正在进行的请求

### 后端 Express 示例

- ✅ **RESTful API**: 完整的用户 CRUD 操作
- ✅ **错误处理**: 统一的错误响应格式
- ✅ **请求日志**: 详细的请求日志记录
- ✅ **随机失败**: 模拟网络问题，测试重试机制
- ✅ **数据验证**: 请求数据格式验证
- ✅ **CORS 支持**: 支持跨域请求
- ✅ **健康检查**: 服务状态监控端点

## 🧪 测试场景

### 1. 基本功能测试

- 在前端界面中创建、查看、删除用户
- 观察请求指标和性能数据
- 测试缓存功能（重复请求应该使用缓存）

### 2. 错误处理测试

```bash
# 测试各种错误场景
curl http://localhost:3001/api/users/test/error?type=500
curl http://localhost:3001/api/users/test/error?type=404
curl http://localhost:3001/api/users/test/error?type=timeout
```

### 3. 重试机制测试

后端有 10% 的随机失败率，可以通过多次创建用户来观察重试机制。

## 📊 API 端点

### 用户管理

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/users` | 获取所有用户 |
| GET | `/api/users/:id` | 获取特定用户 |
| POST | `/api/users` | 创建新用户 |
| PUT | `/api/users/:id` | 更新用户 |
| DELETE | `/api/users/:id` | 删除用户 |

### 系统端点

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/health` | 健康检查 |
| GET | `/` | API 文档 |
| GET | `/api/users/test/error` | 错误测试 |

## 🛠️ 开发脚本

```bash
# 前端开发
pnpm --filter @zhj-sdk/example-frontend-react dev
pnpm --filter @zhj-sdk/example-frontend-react build

# 后端开发
pnpm --filter @zhj-sdk/example-backend-express dev
pnpm --filter @zhj-sdk/example-backend-express build
pnpm --filter @zhj-sdk/example-backend-express start
```

## 📝 注意事项

- 示例应用仅用于演示，不包含生产环境的安全配置
- 后端使用内存存储，重启后数据会丢失
- 随机失败机制仅用于测试，生产环境应移除
- 前端代理配置指向 `http://localhost:3001`

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来改进示例应用！
